#include<bits/stdc++.h>
#define V(all) v.begin(),v.end()
using namespace std;

void OPT()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
}

int main(){
    OPT();
    int t;
    cin >> t;
    while(t--){
        int n, a;
        vector<int> v;
        cin >> n;
        while(n--){
            cin >> a;
            v.push_back(a);
        }
    }
    return 0;
}